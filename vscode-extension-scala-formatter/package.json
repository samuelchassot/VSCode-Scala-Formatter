{
	"name": "scala-formatter",
	"displayName": "Scala code formatter",
	"description": "A language server that formats Scala source code including Dotty Enum syntax using Scalafmt.",
	"author": "Samuel Chassot",
	"publisher": "samuelchassot",
	"version": "1.0.1",
	"repository": {
		"type": "git",
		"url": ""
	},
	"categories": [],
	"keywords": [
		"Scala",
		"code",
		"format"
	],
	"engines": {
		"vscode": "^1.23.0"
	},
	"activationEvents": [
		"onLanguage:scala"
	],
	"main": "./client/out/extension",
	"scripts": {
		"compile": "tsc -b",
		"watch": "tsc -b -w",
		"postinstall": "cd client && npm install && cd .."
	},
	"devDependencies": {
		"typescript": "^3.1.3"
	}
}
